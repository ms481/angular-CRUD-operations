import { ReferenceDataPipe } from './reference-data.pipe';

describe('ReferenceDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenceDataPipe();
    expect(pipe).toBeTruthy();
  });
});
